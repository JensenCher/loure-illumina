import character from "./character-schema";
import category from "./category-schema";
import contact from "./contact-schema";

const schemas = [character, category, contact];

export default schemas;
